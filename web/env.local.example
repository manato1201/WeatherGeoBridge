# Cloudflare Worker(worker/)への到達先とAPIキー。
# サーバーコンポーネント/API Routeでのみ参照し、ブラウザには一切露出しない。
# ローカル開発時は `cd worker && npx wrangler dev --port 8788` に合わせる。
WEATHERGEOBRIDGE_API_BASE_URL=http://localhost:8788
WEATHERGEOBRIDGE_API_KEY=
